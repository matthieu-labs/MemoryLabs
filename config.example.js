// Copy this file to `config.js` and fill in your Supabase project values.
// Settings → API in the Supabase dashboard.
//
// The anon public key is SAFE to expose in a frontend (that's its purpose);
// data is protected by Row Level Security, not by hiding this key.
window.MEMOIR_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-PUBLIC-KEY",
};
